apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80         # The port people visit in the browser
      targetPort: 8080 # The port your app runs on inside the container
  type: LoadBalancer