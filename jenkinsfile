pipeline {
    agent any

    stages {
        stage("git-checkout") {
            steps {
                git branch: 'master', url: 'https://github.com/Sadiq-webapi/git-trianing.git'
            }
        }

        stage("build") {
            steps {
                sh'''
                echo "Building project..."
                env
                '''
            }
        }
 stage("docker-build") {
            steps {
                sh'''
                docker build -t dockerfile .
                '''
            }
        }

     stage("docker") {
    steps {
        sh '''
        docker run -d mynginx
        docker run -d -p 9000:80 dockerfile
        '''
    }
}

    }
}

